<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="SPTarkov.Common" Version="4.0.0-pre.20250912.1958" />
    <PackageReference Include="SPTarkov.DI" Version="4.0.0-pre.20250912.1958" />
    <PackageReference Include="SPTarkov.Reflection" Version="4.0.0-pre.20250912.1958" />
    <PackageReference Include="SPTarkov.Server.Core" Version="4.0.0-pre.20250912.1958" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="WTTCommonLib">
      <HintPath>..\..\..\user\mods\WTTCommonLib\WTTCommonLib.dll</HintPath>
    </Reference>
  </ItemGroup>

  <!-- This runs post build to copy the contents of the resource folder to a build directory -->
  <Target Name="CopyResources" AfterTargets="PostBuildEvent">
    <ItemGroup>
      <Resources Include="$(ProjectDir)Resources\**\*.*" />
    </ItemGroup>

    <!-- Copy task to copy the resources folder to the bin directory -->
    <Copy
            SourceFiles="@(Resources);"
            DestinationFolder="$(ProjectDir)$(OutDir)%(RecursiveDir)"
    />
  </Target>

</Project>
